Scheduled nightly builds are PAUSED.

Reason: the phase-3 redesign (pure versions + build-string lanes, the acpp-llvm
mutex, de-versioned recipes on variants.yaml, acpp-compiler-rt, new publish
gates and retention) has landed on main but has NOT yet had its one-shot CI
run. Until that run is green, a scheduled nightly would spend an hour of the
64-core runner, unattended, proving a tree we already know is unverified — and
on failure it would do so again every day.

Scope: this suppresses `schedule` triggers ONLY. `workflow_dispatch` always
runs, so this never blocks deliberate work.

REMOVE THIS FILE as part of the first green nightly publish on the new scheme.
Deleting it is the last step of the phase-3 cutover, not an afterthought: while
it exists, nightlies only happen when someone asks for one, which is exactly
the wrong steady state for a lane whose whole purpose is a 24h cadence.
