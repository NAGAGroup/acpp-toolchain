# acpp-lldb: the LLDB debugger built against the acpp toolchain LLVM
# (python scripting enabled).
[package]
name = "acpp-lldb"
version = "25.10.0_llvm20.1.8"
publish = true

[package.build]
backend = { name = "pixi-build-rattler-build", version = "*" }
