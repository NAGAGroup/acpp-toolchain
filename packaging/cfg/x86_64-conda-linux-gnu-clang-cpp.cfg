# Clang configuration for the conda target triple, preprocessor driver.
#
# No linker flags: clang-cpp is the pure preprocessor and never links, which is
# the same split conda-forge's clangdev makes.
-isystem <CFGDIR>/../include
--sysroot=<CFGDIR>/../x86_64-conda-linux-gnu/sysroot
