# Nightly build-sets kept INDEFINITELY, exempt from newest-N pruning.
#
# KEEPER POLICY (Jack-ratified 2026-08-09): the first GREEN set of each month
# is kept forever. Keepers layer ON TOP of the newest-N window rather than
# consuming slots in it, so keeping a set never costs a recent one.
#
# A LINE NAMES A SET, NEVER A PACKAGE. The unit here is identical to the unit
# the pruner deletes: a COMPLETE (date, llvm_version) set across every output
# AND both platforms. A partially-kept set is exactly as unsolvable as a
# partially-pruned one, because the nightly lane pins its own packages exactly
# — keep acpp-nightly but drop acpp-runtime-nightly and the survivor's
# dependency can never be satisfied again.
#
# FORMAT: one set per line, "<date>_llvm<version>", '#' starts a comment.
#
#   2026.08.09_llvm21.1.8
#
# WHO WRITES HERE. The monthly policy above is automated: shared/record-keeper.nu
# runs in the `retention` job of nightly.yml, after both platforms have
# published and before anything is pruned, and appends the newest set if — and
# only if — no line already names a set in the same calendar month. It derives
# that set from the published channel repodata, using the same code the pruner
# uses to decide what to delete, so a keeper always names something that was
# really published and really matches.
#
# Add a line BY HAND to promote a set the monthly rule would not catch: a
# nightly a downstream project pinned, or one that reproduced a bug worth
# keeping around. Hand-added lines are indistinguishable from generated ones and
# equally permanent; the month rule counts them, so a hand-added keeper early in
# a month means the recorder writes nothing that month.
#
# READ AT PRUNE TIME, never baked into a snapshot: the pruner re-reads this
# file on every run, so a hand-added keeper protects a set immediately rather
# than at the next release.
#
# Removing a line makes that set eligible for pruning again. Do that only if
# you are certain nothing pins it; there is no undelete.

# --- keepers -----------------------------------------------------------------

# INERT, AND KEPT ON PURPOSE AS A LIVE TEST (Jack, 2026-09-02). This set is no
# longer on the channel — the legacy artifacts were deleted by hand on
# 2026-08-10 — and nothing pins it: the template's acpp branch lock names zero
# 08-09 artifacts, verified against the committed lock. It stays because it is
# last month's, and a stale keeper from a previous month must NOT block this
# month's first green set from being recorded. Deleting this line would remove
# the only case that exercises that.
2026.08.09_llvm21.1.8
# LOAD-BEARING, both platforms. The template's acpp branch has this set in its
# committed lock on linux-64 AND win-64 — 5 outputs each (acpp, acpp-runtime,
# acpp-tools, acpp-clang_<plat>, acpp-clangxx_<plat>), all 10 present on the
# channel. Retire only when that lock no longer references it.
2026.08.10_llvm21.1.8
# First green set of 2026.09, recorded automatically by the retention job (run 33706172179).
2026.09.03_llvm21.1.8
