# Windows (win-64) release lane — dispatch-only while the port matures, so a
# Windows failure never reddens the linux pipeline. Folded into release.yml
# once it has proven itself over a few cycles.
#
# Produces 9 outputs (no acpp-lldb — lldb is not in the win
# LLVM_ENABLE_PROJECTS set; THREE activation packages — clang/clangxx pair
# plus the clang-cl alternative). Publish gate: package content tests (in the
# build) + solver-audit. There is no CI GPU smoke on win yet — Windows GPU
# validation is done manually on real hardware.
name: windows

on:
  workflow_dispatch:

concurrency:
  group: windows-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-win:
    runs-on: Win-x64-64
    timeout-minutes: 300
    env:
      # Everything I/O-heavy lives on D: — the runner's temp drive is ~6x
      # faster than C: for small-file I/O (probed: 4.7k vs 0.8k files/s),
      # which dominates work-dir copies, staging restores and packaging.
      # (Defender is already disabled on the runner image; not a factor.)
      CCACHE_DIR: D:\ccache
      CCACHE_MAXSIZE: 20G
      CCACHE_BASEDIR: D:\rb-output
      CCACHE_NOHASHDIR: "1"
      RATTLER_CACHE_DIR: D:\rattler-cache
      ACPP_OUTPUT_DIR: D:\rb-output
    steps:
      - uses: actions/checkout@v4

      # Must exist before setup-pixi runs: pixi honors RATTLER_CACHE_DIR and
      # errors out if the directory cannot be created (os error 3).
      - name: Prepare D: build directories
        run: New-Item -ItemType Directory -Force -Path D:\rattler-cache, D:\rb-output, D:\ccache | Out-Null

      - uses: prefix-dev/setup-pixi@v0.10.0
        with:
          cache: true
          environments: dev

      # restore/save are SPLIT (the composite actions/cache only saves when
      # the job succeeds) so failed runs still warm the next one — during
      # bring-up loops that is exactly when the cache matters most. Keys use
      # run_id so every run saves a fresh entry; restore falls back by prefix.
      - name: Restore ccache
        uses: actions/cache/restore@v4
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ccache-win-${{ github.run_id }}
          restore-keys: ccache-win-

      - name: Restore rattler cache
        uses: actions/cache/restore@v4
        with:
          path: ${{ env.RATTLER_CACHE_DIR }}
          key: rattler-win-${{ github.run_id }}
          restore-keys: rattler-win-

      # GitHub's Windows runners ship a very long PATH; conda's vs activation
      # then calls vcvars64.bat, which appends more and blows cmd.exe's 8191
      # character limit ("The input line is too long."). conda's own docs say to
      # strip PATH, so keep only what the build needs and let activation
      # rebuild the rest.
      #
      # build-lane.nu selects shared/variants/win-64.yaml from the OS.
      - name: Build release lane (win-64) -> indexed local channel
        run: |
          $env:PATH = @(
            "$env:SystemRoot\system32",
            "$env:SystemRoot",
            "$env:SystemRoot\System32\Wbem",
            "$env:SystemRoot\System32\WindowsPowerShell\v1.0",
            "$env:USERPROFILE\.pixi\bin",
            "$env:ProgramFiles\Git\cmd"
          ) -join ";"
          Write-Host "PATH length: $($env:PATH.Length)"
          pixi run -e dev build-release

      - name: Inventory the produced packages
        shell: bash
        run: |
          find local-channel -name '*.conda' -printf '%f  %s bytes\n' | sort

      # Keep only the newest staging entry, else the cached archive grows
      # without bound across runs.
      - name: Prune stale staging caches
        if: always()
        shell: pwsh
        run: |
          $dir = "D:\rattler-cache"
          if (Test-Path $dir) {
            Get-ChildItem $dir -Filter "staging_*" | Sort-Object LastWriteTime -Descending | Select-Object -Skip 1 | Remove-Item -Recurse -Force
          }

      - name: Save ccache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ccache-win-${{ github.run_id }}

      - name: Save rattler cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: ${{ env.RATTLER_CACHE_DIR }}
          key: rattler-win-${{ github.run_id }}

      - uses: actions/upload-artifact@v4
        with:
          name: local-channel-win-64
          path: local-channel/
          retention-days: 14

  solver-audit:
    needs: build-win
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: local-channel-win-64
          path: local-channel
      - uses: prefix-dev/setup-pixi@v0.10.0
        with:
          cache: true
          environments: dev
      - name: run_exports / run_constraints audits (win-64 solve)
        run: pixi run -e dev audit-win

  publish:
    needs: [build-win, solver-audit]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # OIDC trusted publishing; no stored API key
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: local-channel-win-64
          path: local-channel
      - uses: prefix-dev/setup-pixi@v0.10.0
        with:
          cache: true
          environments: dev
      - name: Upload to prefix.dev
        run: pixi run -e dev publish-channel
